
<%= form_tag (@comments) do |f| %>

	<p>
		<%= f.text_field :title %> <br>
		<%= f.text_area :title %>
	</p>
	<p>
		<%= f.text_field :body %> <br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>