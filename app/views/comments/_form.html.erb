<%= form_for([@user, @user.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    current user session placeholder
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
